.DS_Store
*.swp
*.bak
__pycache__/
.pytest_cache/
.mypy_cache/

# lib/_store.sh is copied at install time, not source-tracked
lib/_store.sh
apctest.output

# Local Claude settings (may contain personal permission overrides)
.claude/settings.local.json

# SDD workflow scratch (briefs, reports, review diffs, ledger)
.superpowers/

# This repo's own dev-tooling venv (built from requirements-dev.txt).
# Only types-guard.sh and run-tests.sh prefer it; everything else in the repo
# is shipped code that must keep resolving python3 from the ambient PATH on
# whatever host installs it.
.venv/
