# Ticket notes

## No dev file

There is no dev items file because dev has no data for this customer to resolve
against — `WHERE customer_no='JP-BA'` returns nothing in dev. Mechanism testing
used an ad-hoc file against a disposable account instead.
